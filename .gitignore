node_modules
.next
out
build
.DS_Store

# Environment
.env
.env.*
!.env.example

# Supabase (로컬 + 서버 키/시크릿 제외 — 마이그레이션 스키마는 유지)
supabase/.env
supabase/.env.local
supabase/config.toml

# Anchor / Solana
target
programs/*/target
*.keypair.json
.anchor

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# 시연 영상 스크립트 (팀 내부 자료 — 버전관리 제외)
docs/demo_script.md